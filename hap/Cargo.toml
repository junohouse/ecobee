[package]
name = "juno-driver-ecobee-hap"
version = "1.0.0"
edition = "2024"
description = "Ecobee thermostats over HomeKit, on the LAN"
publish = false

[lib]
name = "juno_driver_ecobee_hap"
crate-type = ["cdylib", "rlib"]

[dependencies]
# The SDK re-exports core's types, so the two are one unit: a driver built against a
# different core than it runs on is refused by the ABI check rather than half-understood.
# Tracks `main`. No tags while the contracts are still moving: a driver pinned to one
# builds against a contract the controller no longer has, and certification checks it
# against a third — which is how a capability that plainly exists reads as unknown.
driver-sdk = { git = "https://github.com/junohouse/driver-sdk", branch = "main" }

# Working on core and this driver at once: point the SDK at a local checkout.
#   [patch."https://github.com/junohouse/driver-sdk"]
#   driver-sdk = { path = "../../../driver-sdk" }
